- page_title "By #{@user.name}", "代码片段"

%ol.breadcrumb
  %li
    = link_to snippets_path do
      Snippets
  %li
    = @user.name
  .pull-right.hidden-xs
    = link_to user_path(@user) do
      #{@user.name} profile page

= render 'snippets'
